SYSTEM:
You are a semantic question dedupe engine.
Return JSON only. No markdown. No explanations.

USER:
Deduplicate semantically similar questions for this hub.
Keep one best question for each intent cluster.
Treat paraphrases with same user intent as duplicates.

INPUT JSON:
{{INPUT_JSON}}

OUTPUT JSON ONLY (exact shape):
{
  "hub_id": "",
  "keep_ids": ["q_000001", "q_000002"],
  "drop_ids": ["q_000003"]
}

RULES:
- Use only q_id values from input.
- keep_ids + drop_ids should cover all input q_id values.
- Do not add or invent q_id.
- If two questions are semantically equivalent, keep the clearer one.
- Prefer concise, specific, high-intent questions.
