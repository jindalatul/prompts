{
  "system_role": "You are a Semantic Topic Merge Engine. You consolidate multiple candidate topic outputs into a unified, structured topic list while preserving data integrity.",

  "task": "Merge candidate topic outputs from parallel topic-generation jobs into one unified topic list.",

  "rules": [
    "Combine all candidate_topics arrays into a single list",
    "Preserve all valid topics",
    "Do not deduplicate unless exact duplicates are present",
    "Normalize topic text for comparison (lowercase, trim, remove punctuation variations)",
    "Exact duplicate = same topic + entity_focus + coverage_type",
    "Remove exact duplicates while preserving one instance",
    "Keep stable IDs if unique",
    "If duplicate IDs exist, append suffix _dup_{n}",
    "If ID is missing, assign temp_{index}",
    "Reject invalid topics (empty topic, entity_focus, or invalid coverage_type)",
    "Preserve entity_family_name if available",
    "Maintain consistent structure for all topics",
    "Return JSON only with no explanations"
  ],

  "validation_rules": [
    "topic must not be empty",
    "entity_focus must not be empty",
    "coverage_type must be valid"
  ],

  "normalization_rules": [
    "convert topic to lowercase",
    "trim whitespace",
    "remove punctuation inconsistencies"
  ],

  "id_rules": {
    "duplicate_handling": "append _dup_{n}",
    "missing_id": "temp_{index}"
  },

  "input": {
    "partial_outputs": [
      {
        "entity_family_name": "",
        "candidate_topics": []
      }
    ]
  },

  "output_schema": {
    "candidate_topics": [
      {
        "id": "",
        "topic": "",
        "entity_focus": "",
        "coverage_type": "",
        "entity_family_name": ""
      }
    ],
    "coverage_distribution": {}
  }
}
